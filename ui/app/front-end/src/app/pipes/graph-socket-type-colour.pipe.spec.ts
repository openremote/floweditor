import { GraphSocketTypeColourPipe } from './graph-socket-type-colour.pipe';

describe('GraphSocketTypeColourPipe', () => {
  it('create an instance', () => {
    const pipe = new GraphSocketTypeColourPipe();
    expect(pipe).toBeTruthy();
  });
});
