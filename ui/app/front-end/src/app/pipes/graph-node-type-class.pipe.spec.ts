import { GraphNodeTypeClassPipe } from './graph-node-type-class.pipe';

describe('GraphNodeTypeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new GraphNodeTypeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
