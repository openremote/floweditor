import { GraphNodeNamePipe } from './graph-node-name.pipe';

describe('GraphNodeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GraphNodeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
